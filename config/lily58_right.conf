# Right side specific configuration for Lily58
# Each half sleeps independently based on its own idle timer.

# Needs to be enabled for both
CONFIG_ZMK_SLEEP=y

# 1800000 ms = 30 minutes (matches lily58.conf sleep timeout)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000
